'use strict';

const testSolution = require('./index.js');
require('jest');

describe('testing the function solution', function () {
  // test('What is being tested?', () => {
  //   expect(testSolution()).toBe();
  // });
  test('What is truth?', () => {
    expect(true).toBeTruthy();
  });
});